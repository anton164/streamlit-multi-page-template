This template scaffolds a multi-page streamlit app. Each sub-page is a separate streamlit app.

### Run multi-page app
```
streamlit run app.py
```

### ...or run each interface separately
```
streamlit run interface/page1.py
```